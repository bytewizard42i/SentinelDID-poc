{
    "name": "sentineldid-contract-ui",
    "version": "1.0.0",
    "scripts": {
        "compile": "compactc compile SentinelDID.compact",
        "deploy": "midnight-cli deploy SentinelDID.compact --network testnet"
    }
}